# 应用运行环境，可根据需要切换为 development / staging / production
APP_ENV=development

# 调试专用开关，开启后可以输出 DEBUG 级别日志
APP_TEST_MODE=false

# REST API 的版本前缀，除非自定义路由前缀一般无需修改
API_V1_STR=/api/v1

# 项目名称，用于日志与文档显示
PROJECT_NAME=PromptWorks

# PostgreSQL 数据库连接串，本地默认使用 promptworks 账户与数据库
DATABASE_URL=postgresql+psycopg://promptworks:promptworks@localhost:5432/promptworks

# Redis 连接地址，当前用于后续的任务队列或缓存功能，可先保持默认
REDIS_URL=redis://localhost:6379/0

# 允许跨域访问的前端地址，可使用逗号分隔多个 URL
BACKEND_CORS_ORIGINS=["http://localhost:5173"]

# 是否允许跨域请求携带 Cookie 或认证信息
BACKEND_CORS_ALLOW_CREDENTIALS=true

# JWT 认证所需密钥与访问令牌过期时间（分钟）
SECRET_KEY=change-me
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# OpenAI 与 Anthropic 的 API Key，可在需要调用对应模型时填写
OPENAI_API_KEY=
OPENAI_BASE_URL=
ANTHROPIC_API_KEY=
ANTHROPIC_BASE_URL=
